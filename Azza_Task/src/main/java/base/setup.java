package base;


public class setup {
	public static void openBrowser(String browser,String url) {
		setup.openBrowser(browser,url);
	}
	public static void closeBrowser(String name) {
		setup.closeBrowser(name);
	}
}
