<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">
<suite parallel="tests" thread-count="30" name="AzzaTask">
	<parameter name="hostOs" value="${host}"></parameter>
	<parameter name="browser" value="${browser.name}"></parameter>
	<parameter name="env" value="${environment}" />
	<parameter name="targetApp" value="${base.url}" />
	<parameter name="PortabilityApp" value="${portability.url}" />
	<parameter name="password" value="${password}" />
	<parameter name="hubUrl" value="${hub.url}"></parameter>
	<parameter name="enableHAR" value="${enable.har}"></parameter>
 	<parameter name="dryRun" value="${dry.run}"></parameter> 
 	<parameter name="enableAllureServer" value="${enable.allureServer}"></parameter> 	
	<test name="testcase1">
		<parameter name="username" value="${user.name24}" />
		<classes>
			<class name="tests.testcase1Test" />
		</classes>
	</test> 
</suite> 
